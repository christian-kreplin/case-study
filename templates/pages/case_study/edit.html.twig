{% set actionBtn = {link: path('app_case_study_add'), label: 'Case Study erstellen'} %}
{% extends 'layouts/base.html.twig' %}

{% block body %}
  <div class="form-wrapper">
    {{ form_start(form, {attr: {class: 'manage more-space'}}) }}
    {% include 'components/pages/item_header.html.twig' with { backLink: path('app_case_study_browse')} %}

    <div class="fields">
      <div class="field">
        {{ form_widget(form.customer) }}
        {{ form_label(form.customer) }}
      </div>

      <div class="field">
        <input type="text"
               value="{{ field_value(form.title ) }}"
               name="{{ field_name(form.title ) }}"
               id="{{ field_name(form.title ) }}"
               class="default peer"
               placeholder=" "
               required>
        {% include 'components/form/label_for_input.html.twig' with {id: field_name(form.title ), text: field_label(form.title )} %}
      </div>

      <div class="field">
        <div class="custom-label">
          {{ form_label(form.description) }}
        </div>
        {{ form_widget(form.description) }}
      </div>

      <div class="field">
        <div class="custom-label">
          {{ form_label(form.imageFile) }}
        </div>
        {{ form_widget(form.imageFile) }}
      </div>
    </div>
    {{ form_end(form) }}
  </div>

  {% if errors is defined %}
    {% include 'components/dialog/dialog.html.twig' with {title: (errors|length == 1 ? 'Problem' : 'Probleme') ~ ' aufgetreten', message: errors, buttonText: 'OK'} %}
  {% endif %}
{% endblock %}